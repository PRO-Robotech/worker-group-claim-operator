# This rule is not used by the project worker-group-claim-operator itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants permissions to create, update, and delete resources within the workergroup.in-cloud.io.
# This role is intended for users who need to manage these resources
# but should not control RBAC or manage permissions for others.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: worker-group-claim-operator
    app.kubernetes.io/managed-by: kustomize
  name: wgbootstraptemplate-editor-role
rules:
- apiGroups:
  - workergroup.in-cloud.io
  resources:
  - wgbootstraptemplates
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - workergroup.in-cloud.io
  resources:
  - wgbootstraptemplates/status
  verbs:
  - get
