---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: wgmachinetemplates.workergroup.in-cloud.io
spec:
  group: workergroup.in-cloud.io
  names:
    kind: WGMachineTemplate
    listKind: WGMachineTemplateList
    plural: wgmachinetemplates
    singular: wgmachinetemplate
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          WGMachineTemplate is a cluster-scoped Go template that produces BegetMachineTemplate resources.
          Referenced by WorkerGroupClaim.spec.machineTemplateRef.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: WGMachineTemplateSpec defines the desired state of WGMachineTemplate.
            properties:
              value:
                description: |-
                  Value is the Go template content that produces a BegetMachineTemplate.
                  Variables from WorkerGroupClaim.spec.infrastructure are passed as template data.
                minLength: 1
                type: string
            required:
            - value
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
